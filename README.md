# project-acess

## Login

- **Admin dashboard access**: `username=admin` / `password=admin`
- **Student/User access**: use `username=<identity_id>` and temporary password `<YYYYMMDD of DOB>` on first login.
- On first login, non-admin users must change their password before access is granted.
